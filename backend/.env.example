# AI Provider API Keys
DEEPSEEK_API_KEY=your_deepseek_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Server Configuration (Docker optimized)
HOST=0.0.0.0
PORT=8000
DEBUG=False

# CORS settings (Docker networks)
CORS_ORIGINS=http://localhost,http://localhost:3000,http://localhost:8080,http://frontend

# Database/Storage paths (Docker volumes)
DATA_PATH=/app/data
LOGS_PATH=/app/logs

# Security
SECRET_KEY=change_this_to_random_secret_key_in_production

# Redis (optional, for caching)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=defaultpassword

# Production settings
ENVIRONMENT=production
