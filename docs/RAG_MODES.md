# RAG Modes Documentation

## Обзор режимов RAG

Система поддерживает **10 режимов** RAG поиска, каждый оптимизирован для разных типов запросов.

### 🧠 `smart` - УМНЫЙ RAG (РЕКОМЕНДУЕТСЯ) 🚀

**AI-driven понимание любых запросов к документам!**

Smart RAG автоматически понимает ЛЮБОЙ запрос на ЛЮБОМ языке:

* "расскажи о 40 главе" ✅
* "сороковая глава" ✅ (словом!)
* "tell me about chapter forty" ✅ (английский словом!)
* "сравни главы 1 и 5" ✅
* "перескажи всю книгу" ✅
* "найди лазейки в законе" ✅
* "в чем противоречие между статьями?" ✅

**Как работает:**

1. AI анализирует запрос и определяет:
   * **Scope**: одна глава, несколько глав, весь документ, поиск
   * **Task**: пересказ, анализ, сравнение, поиск лазеек
   * **Sections**: какие именно главы/разделы нужны
2. Загружает нужный контекст (может загрузить целую главу или весь документ)
3. Добавляет task-specific инструкции для модели

**Когда использовать:** ВСЕГДА как основной режим!

### 1. `off` - Отключен

RAG не используется. Модель отвечает только на основе своих знаний.

### 2. `auto` - Автоматический (по умолчанию)

Автоматически включает RAG если у пользователя есть документы.
Использует hybrid search с reranking.

**Когда использовать:** Всегда по умолчанию.

### 3. `basic` - Базовый

Простой hybrid search (векторный + keyword). Быстрый, но может пропустить релевантные документы.

**Когда использовать:** Простые вопросы, когда скорость важнее точности.

### 4. `advanced` - Продвинутый

Multi-query retrieval + hybrid search + reranking через LLM.

**Техники:**

* Генерация 3-4 вариантов запроса
* Поиск по каждому варианту
* LLM reranking для точности

**Когда использовать:** Стандартные вопросы по документам.

### 5. `ultimate` - Ультиматум (Авто-стратегия)

Автоматически выбирает лучшую стратегию на основе анализа запроса.

**Автодетект:**

* Специфичные запросы (главы, цитаты, страницы) → HyDE
* Широкие запросы (о чем, резюме) → Multi-query
* Остальные → Multi-query (дефолт)

**Когда использовать:** Когда не знаете какой режим лучше.

### 6. `hyde` - HyDE (Hypothetical Document Embeddings)

Сначала генерирует гипотетический ответ, затем ищет похожие документы.

**Техники:**

* Step-back prompting (обобщенный вопрос)
* Генерация гипотетического документа
* Поиск по гипотетическому ответу
* LLM reranking

**Когда использовать:**

* "Процитируй первый абзац 14 главы"
* "Что написано на странице 50?"
* Любые структурные запросы

**Почему работает лучше для структуры:**
Вопрос "процитируй главу 14" не имеет семантического сходства с текстом главы.
HyDE сначала генерирует: "В главе 14 автор описывает..." и ищет по этому тексту.

### 6. `agentic` - Агентный RAG

LLM-агент сам решает какие запросы делать, итеративно собирая информацию.

**Как работает:**

1. Агент анализирует вопрос
2. Генерирует поисковый запрос
3. Получает результаты
4. Решает нужно ли еще искать
5. Повторяет до 3 раз или пока не соберет достаточно

**Когда использовать:**

* Сложные многошаговые вопросы
* "Сравни главу 5 и главу 10"
* Вопросы требующие информацию из разных частей документа

### 7. `full` - Полный документ 📚

Загружает ВЕСЬ документ в контекст модели.

**Особенности:**

* Загружает до 100K токенов (~400K символов)
* Требует модели с большим контекстом (Gemini 1.5 Pro 1M, Claude 200K, GPT-4o 128K)
* Идеален для анализа всей книги

**Когда использовать:**

* "Напиши краткое содержание всей книги"
* "Перечисли всех персонажей"
* "Проанализируй стиль автора"
* "Сколько глав в книге?"

**Ограничения:**

* ⚠️ Очень много токенов = дорого
* ⚠️ Медленнее из-за большого контекста
* ⚠️ Output лимит модели (8-16K) не позволит переписать всю книгу за раз

### 8. `chapter` - По главам 📖

Работа с конкретной главой документа.

**Особенности:**

* Автоматически определяет главы в документе
* Может автоматически найти главу из запроса ("перескажи главу 3")
* Загружает ~30K токенов на главу

**Когда использовать:**

* "Перескажи главу 5"
* "О чем глава 10?"
* "Переписать главу 3 своими словами"
* Итеративная работа с книгой по главам

**API для глав:**

```
GET /rag/documents/{id}/chapters - список глав
GET /rag/documents/{id}/chapter/{num} - содержимое главы
```

## Сравнение режимов

| Режим | Скорость | Точность | Токены | Лучше для |
|-------|----------|----------|--------|-----------|
| **🧠 smart** | ⚡⚡ | ⭐⭐⭐⭐⭐ | **~50K** | **ВСЁ! Рекомендуется** |
| auto | ⚡⚡⚡ | ⭐⭐⭐ | ~8K | По умолчанию |
| basic | ⚡⚡⚡ | ⭐⭐ | ~4K | Быстрые простые вопросы |
| advanced | ⚡⚡ | ⭐⭐⭐ | ~8K | Стандартные вопросы |
| ultimate | ⚡⚡ | ⭐⭐⭐⭐ | ~10K | Авто-выбор стратегии |
| hyde | ⚡ | ⭐⭐⭐⭐⭐ | ~10K | Структурные запросы |
| agentic | ⚡ | ⭐⭐⭐⭐⭐ | ~15K | Сложные вопросы |
| **full** | 🐢 | ⭐⭐⭐⭐⭐ | **100K+** | Анализ всей книги |
| **chapter** | ⚡⚡ | ⭐⭐⭐⭐⭐ | **~30K** | Работа по главам |

## Рекомендации по книгам с 200k+ токенов

### Для анализа/вопросов по всей книге:

Используйте **`full`** режим + модель с большим контекстом:

* Gemini 1.5 Pro (1M токенов) ✅ Лучший выбор
* Claude 3.5 Sonnet (200K) ✅ Хорошо
* GPT-4o (128K) ⚠️ Может не хватить

### Для переписывания/пересказа:

Используйте **`chapter`** режим итеративно:

1. Запрос: "Перескажи главу 1"
2. Запрос: "Перескажи главу 2"
3. И так далее...

**Почему?** Output лимит моделей (8-16K токенов) не позволяет выдать 200K текста за раз.

### Для структурных запросов:

Используйте **`hyde`** режим:

* "Процитируй первый абзац 14 главы"
* "Что написано на странице 50?"

### Для сложных вопросов по нескольким частям:

Используйте **`agentic`** режим:

* "Сравни тему X в главах 5 и 10"
* "Перечисли всех персонажей и их роли"

## UI Отображение

В интерфейсе под каждым ответом с RAG показывается:

* Количество найденных чанков
* Использованная стратегия
* Техники (multi\_query, hyde, rerank, etc)
* Сгенерированные подзапросы
* История поиска агента (для agentic)
* Preview контекста отправленного модели

## Примеры конфигурации

### Автоматический режим (по умолчанию)

```typescript
const ragConfig: RAGConfig = {
  enabled: true,
  mode: 'auto'
};
```

### Полный документ

```typescript
const ragConfig: RAGConfig = {
  enabled: true,
  mode: 'full'
};
```

### Работа по главам

```typescript
const ragConfig: RAGConfig = {
  enabled: true,
  mode: 'chapter'
};
// Запрос: "Перескажи главу 3" - автоматически найдет нужную главу
```

### Максимальное качество поиска

```typescript
const ragConfig: RAGConfig = {
  enabled: true,
  mode: 'ultimate',  // или 'hyde', 'agentic'
  use_rerank: true,
  min_similarity: 0.3,
  max_chunks: 15
};
```

## Debug Mode - Просмотр полного промпта

Для отладки и понимания что именно отправляется в модель, используйте `debug_mode`:

```typescript
const ragConfig: RAGConfig = {
  enabled: true,
  mode: 'full',
  debug_mode: true  // Показывает ВЕСЬ промпт отправленный модели
};
```

**Что показывает debug\_mode:**

* `system_prompt_full` - ПОЛНЫЙ системный промпт включая RAG контекст
* `history_messages` - Историю сообщений отправленную в модель
* `rag_context_full` - Полный RAG контекст (все чанки)
* `history_count` - Количество сообщений в истории

**Когда использовать:**

* Понять почему модель не видит нужную информацию
* Проверить что RAG находит правильные чанки
* Отладка проблем с контекстом
* Проверка сжатия истории
